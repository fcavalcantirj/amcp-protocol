{
  "category": "amcp-protocol",
  "description": "Generate AMCP identity (AID + keypairs)",
  "steps": [
    "createIdentity() -> { aid, publicKey, privateKey, created }",
    "Generate Ed25519 keypair for signing",
    "AID = Base64URL encode of public key (like KERI)",
    "Store identity in ~/.amcp/identity.json:",
    "  { aid, publicKey, privateKey, created: ISO8601 }",
    "Private key NEVER leaves this file (or encrypted backup)",
    "Public key can be shared (used for signature verification)",
    "CLI: amcp identity create --out ~/.amcp/identity.json",
    "CLI: amcp identity show (displays AID and public key only)",
    "Verify: Generated keypair is valid Ed25519",
    "Verify: AID derivable from public key",
    "Verify: Private key stored securely (600 permissions)"
  ],
  "passes": false
}
