{
  "category": "amcp-protocol",
  "description": "Signature scheme for checkpoint authenticity",
  "steps": [
    "sign(data: string, privateKey: string) -> signature: string",
    "  1. Ed25519 sign the data bytes",
    "  2. Return base64url encoded signature",
    "verify(data: string, signature: string, publicKey: string) -> boolean",
    "  1. Decode base64url signature",
    "  2. Ed25519 verify against data bytes",
    "  3. Return true if valid, false if not",
    "publicKeyFromAID(aid: string) -> publicKey",
    "  AID is base64url of public key, so decode it",
    "For checkpoints:",
    "  - Sign the encrypted payload (base64 string)",
    "  - Signature proves: only holder of private key created this",
    "  - On resuscitate: verify BEFORE decrypting",
    "  - Invalid signature = reject (possible injection attack)",
    "Verify: sign→verify returns true",
    "Verify: Tampered data fails verification",
    "Verify: Wrong key fails verification"
  ],
  "passes": false
}
