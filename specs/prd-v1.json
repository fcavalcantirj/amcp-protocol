[
  {
    "category": "validation",
    "description": "Add JSON schema validation to loadIdentity() — reject identity.json missing required KERI fields",
    "steps": [
      "Locate loadIdentity() in scripts/amcp-cli.ts (line 106-127)",
      "Current state: parses JSON, detects old format (agent/chain) vs new format (flat) at lines 111-123, but no field-level schema check",
      "Add schema check after format detection: require aid (string), kel (array with >=1 event), currentPrivateKey (string)",
      "Throw descriptive error on missing/wrong-typed fields: 'Invalid identity: missing agent.aid (expected KERI AID string)'",
      "Verify: loading openclaw-deploy-style flat identity (sha256 aid, secrets.pinata_jwt, no kel) throws immediately"
    ],
    "passes": true,
    "phase": 1
  },
  {
    "category": "validation",
    "description": "Add Ed25519 on-curve validation to isValidAid() — reject AIDs that aren't valid Ed25519 public keys",
    "steps": [
      "Locate isValidAid() in packages/amcp-core/src/aid.ts (line 48-55)",
      "Current state: calls publicKeyFromAid() which checks ED25519_PREFIX ('B') + base64url decode + 32-byte length — no curve check",
      "Add Ed25519 on-curve validation using existing @noble/ed25519 dependency (imported in crypto.ts lines 7-11)",
      "Reject AIDs where decoded bytes are not a valid Ed25519 point (e.g. sha256 hash output passes length check but fails curve check)",
      "Export isValidAid from packages/amcp-core/src/index.ts (currently missing from exports)",
      "Verify: sha256-derived AID from openclaw-deploy is rejected"
    ],
    "passes": true,
    "phase": 1
  },
  {
    "category": "validation",
    "description": "Add 'amcp identity validate' CLI command — exit 0 if valid KERI, exit 1 with errors if not",
    "steps": [
      "Add 'identity validate' subcommand to if-else chain in scripts/amcp-cli.ts (lines 339-460)",
      "Existing commands: identity create, identity show, checkpoint create, resuscitate, verify",
      "Accept optional --path flag (defaults to ~/.amcp/identity.json)",
      "Run full validation: schema check, AID crypto check, KEL integrity check",
      "Output structured result: { valid: true } or { valid: false, errors: [...] }",
      "Exit code 0 on valid, 1 on invalid — designed for scripting use by proactive-amcp and openclaw-deploy"
    ],
    "passes": true,
    "phase": 1
  },
  {
    "category": "validation",
    "description": "Add KEL integrity check to loadIdentity() — call verifyKEL() on load, reject corrupt/missing inception events",
    "steps": [
      "Cross-package gap: loadIdentity() is in scripts/amcp-cli.ts, verifyKEL() is in packages/amcp-core/src/kel.ts (line 182) — different packages",
      "Option A: import verifyKEL from @amcp/core in the CLI script (already exported from index.ts)",
      "Option B: inline minimal KEL check in CLI if import is impractical",
      "verifyKEL() validates: non-empty KEL, inception event at sn=0, event signatures, chain integrity, rotation key commitments (lines 182-228)",
      "Call after schema validation in loadIdentity(), reject if KEL is empty, missing inception, or has invalid signature"
    ],
    "passes": true,
    "phase": 1
  },
  {
    "category": "api",
    "description": "Export validateIdentityFile(path): {valid, errors[]} from @amcp/core — reusable validation for any consumer",
    "steps": [
      "Create packages/amcp-core/src/validate.ts with validateIdentityFile(path) function",
      "Closest existing: loadAgent() in agent.ts (line 125) validates KEL+keypair but takes SerializedAgent object, not file path",
      "Compose all checks: file exists, valid JSON, schema validation, AID crypto check (isValidAid), KEL integrity (verifyKEL)",
      "Return { valid: true, errors: [] } or { valid: false, errors: ['AID is not valid Ed25519', ...] }",
      "Export from packages/amcp-core/src/index.ts",
      "Verify: import { validateIdentityFile } from '@amcp/core' works"
    ],
    "passes": true,
    "phase": 1
  },
  {
    "category": "testing",
    "description": "Add test cases for fake identity rejection — verify openclaw-deploy-style identity fails every validation layer",
    "steps": [
      "Create packages/amcp-core/src/test/validate.test.ts using Vitest (see agent.test.ts for pattern)",
      "Create test fixture: fake identity JSON matching openclaw-deploy format (sha256 aid, secrets.pinata_jwt, no agent wrapper, no kel)",
      "Test loadIdentity() rejects fake identity with schema error",
      "Test isValidAid() rejects sha256-derived AID with crypto error (fails on-curve check)",
      "Test validateIdentityFile() returns { valid: false } with multiple errors for fake identity",
      "Test valid KERI identity passes all checks (positive test — use createAgent() to generate valid fixture)"
    ],
    "passes": true,
    "phase": 1
  },
  {
    "category": "ontology",
    "description": "Extend CheckpointMetadata interface — add ontologyGraphCID, soulHash, reconstructionSeam fields",
    "steps": [
      "Locate CheckpointMetadata interface in packages/amcp-memory/src/checkpoint.ts (currently around line 15-25)",
      "Current fields: version, aid, timestamp, contentHash, secretsEncrypted, previousCID, signature",
      "Add optional fields: ontologyGraphCID?: string (CIDv1 of graph.jsonl), soulHash?: string (SHA256 of SOUL.md), reconstructionSeam?: number (0-7, loading step when identity coalesces)",
      "Update createCheckpoint() function (line 40-80) to accept these fields in metadata parameter",
      "Add JSDoc comments explaining: ontologyGraphCID is CIDv1 of memory/ontology/graph.jsonl (omit if no ontology), soulHash is SHA256 for drift detection, reconstructionSeam is phenomenological marker (default: 7 = post-context-load)",
      "Verify: TypeScript compilation succeeds, existing checkpoints without new fields remain valid (backward compatible)"
    ],
    "passes": false,
    "phase": 2
  },
  {
    "category": "ontology",
    "description": "Add computeGraphCID() utility — compute CIDv1 of JSONL graph files",
    "steps": [
      "Create packages/amcp-memory/src/graph-cid.ts (<100 lines) with computeGraphCID(filePath: string): Promise<string>",
      "Use multiformats library (add to package.json dependencies: 'multiformats': '^13.0.0')",
      "Read JSONL file, compute SHA256 hash of content, create CID using multiformats: import { CID } from 'multiformats/cid'; import * as raw from 'multiformats/codecs/raw'; import { sha256 } from 'multiformats/hashes/sha2'",
      "CID format: raw codec (0x55), sha256 hash (0x12), CIDv1, base32 encoding (produces 'bafkrei...' prefix)",
      "Handle edge cases: missing file returns null, empty file returns CID of empty string, validate each line is valid JSON before hashing",
      "Export from packages/amcp-memory/src/index.ts: export { computeGraphCID } from './graph-cid'",
      "Verify: computeGraphCID() produces deterministic CIDs (same file content = same CID), matches IPFS CID computation"
    ],
    "passes": false,
    "phase": 2
  },
  {
    "category": "ontology",
    "description": "Define ontology entity types — TypeScript types for typed graphs",
    "steps": [
      "Create packages/amcp-memory/src/ontology-types.ts (<200 lines) with entity and relation type definitions",
      "Base types: Entity { id: string; type: EntityType; properties: Record<string,any>; created: Date; updated: Date }, Relation { from_id: string; relation_type: RelationType; to_id: string; properties?: Record<string,any> }",
      "Entity types enum: Person, Organization, Project, Task, Goal, Event, Location, Document, Message, Note, Account, Credential, Action, Policy",
      "Relation types enum: has_owner, owns, has_task, part_of, member_of, blocks, depends_on, mentions, references",
      "Concrete types: PersonEntity extends Entity { type: 'Person'; properties: { name: string; email?: string; phone?: string; notes?: string } }",
      "Schema constraints interface: EntityConstraint { type: EntityType; required_properties: string[]; forbidden_properties?: string[]; acyclic_relations?: RelationType[] }",
      "Export all types from packages/amcp-memory/src/index.ts",
      "Verify: TypeScript strict mode passes, all entity types have proper type safety"
    ],
    "passes": false,
    "phase": 2
  },
  {
    "category": "ontology",
    "description": "Add validateOntologySchema() function — validate JSONL graph files",
    "steps": [
      "Create packages/amcp-memory/src/validate-ontology.ts (<150 lines) with validateOntologySchema(filePath: string): Promise<ValidationResult>",
      "ValidationResult type: { valid: boolean; entity_count: number; relation_count: number; errors: string[] }",
      "Validation checks: 1) Each line is valid JSON, 2) Each object has 'type' field (entity or relation), 3) Entities have required fields (id, type, properties, created, updated), 4) Relations have required fields (from_id, relation_type, to_id), 5) Relation integrity (from_id and to_id reference existing entity IDs), 6) Acyclic check for 'blocks' and 'depends_on' relations (detect cycles via DFS)",
      "Build entity index while parsing: Map<string, Entity> for O(1) relation validation",
      "Cycle detection: for each 'blocks' relation, run DFS from to_id, error if reaches from_id",
      "Return detailed errors: 'Line 42: Relation from_id='task-123' references non-existent entity', 'Line 58: Circular dependency detected: task-A blocks task-B blocks task-A'",
      "Export from packages/amcp-memory/src/index.ts",
      "Verify: validateOntologySchema() rejects invalid graphs, accepts valid graphs, detects circular dependencies"
    ],
    "passes": false,
    "phase": 2
  },
  {
    "category": "ontology",
    "description": "SOUL drift detection utilities — computeSoulHash(), detectSoulDrift() with severity classification",
    "steps": [
      "Create packages/amcp-core/src/soul-drift.ts (<200 lines) with SOUL identity change detection utilities",
      "computeSoulHash(soulContent: string): string — returns SHA256 hash of SOUL.md content (use @noble/hashes/sha256)",
      "detectSoulDrift(previousHash: string, currentHash: string, previousContent: string, currentContent: string): DriftResult",
      "DriftResult type: { drifted: boolean; severity: 'none' | 'minor' | 'moderate' | 'major'; lines_changed: number; diff_snippet?: string }",
      "Severity rules: 0 lines = none, 1-4 = minor, 5-20 = moderate, >20 = major",
      "Use diff algorithm: import { diffLines } from 'diff' (add 'diff' to package.json dependencies), count changed lines (insertions + deletions)",
      "Include first 5 lines of unified diff in diff_snippet for logging",
      "Export from packages/amcp-core/src/index.ts",
      "Verify: detectSoulDrift() correctly classifies severity, diff computation matches command-line diff output"
    ],
    "passes": false,
    "phase": 2
  },
  {
    "category": "ontology",
    "description": "Reconstruction sequence utilities — standardize memory loading order API",
    "steps": [
      "Create packages/amcp-recovery/src/reconstruction.ts (<150 lines) defining canonical reconstruction sequence",
      "ReconstructionStep enum: SOUL = 0, USER = 1, ONTOLOGY_SCHEMA = 2, ONTOLOGY_GRAPH = 3, CURATED_MEMORY = 4, EPHEMERAL_NOTES = 5, TOOLS = 6, SEAM = 7",
      "loadMemorySequence(checkpointPath: string, outputDir: string): Promise<ReconstructionProgress> — orchestrates loading in canonical order",
      "ReconstructionProgress type: { current_step: ReconstructionStep; steps_completed: ReconstructionStep[]; seam_crossed: boolean; errors: string[] }",
      "Implementation: 1) Load SOUL.md -> 2) Load USER.md -> 3) Load ontology schema (if exists) -> 4) Validate and load graph.jsonl -> 5) Load MEMORY.md -> 6) Load daily notes -> 7) Load TOOLS.md -> 8) Set seam_crossed: true",
      "Each step validates before proceeding: missing SOUL.md fails immediately (identity required), missing ontology skips steps 3-4 (optional)",
      "Export from packages/amcp-recovery/src/index.ts",
      "Verify: loadMemorySequence() follows canonical order, seam_crossed only true after all steps complete, missing optional files don't block progression"
    ],
    "passes": false,
    "phase": 2
  },
  {
    "category": "testing",
    "description": "Unit tests for ontology validation — entity and relation schema enforcement",
    "steps": [
      "Create packages/amcp-memory/src/test/validate-ontology.test.ts using Vitest",
      "Test 1: Valid graph with 2 entities + 1 relation passes validation (valid: true, no errors)",
      "Test 2: Invalid JSON on line 3 fails with parse error",
      "Test 3: Entity missing 'id' field fails with required field error",
      "Test 4: Relation with from_id referencing non-existent entity fails with integrity error",
      "Test 5: Circular dependency (task-A blocks task-B blocks task-A) fails with cycle detection error",
      "Test 6: Empty file passes validation (valid: true, entity_count: 0, relation_count: 0)",
      "Test 7: Mixed valid/invalid lines returns errors for invalid lines only",
      "Run: pnpm test validate-ontology.test.ts",
      "Verify: all 7 tests pass, error messages are descriptive"
    ],
    "passes": true,
    "phase": 1
  },
  {
    "category": "testing",
    "description": "Unit tests for SOUL drift — identity change severity classification",
    "steps": [
      "Create packages/amcp-core/src/test/soul-drift.test.ts using Vitest",
      "Test 1: Identical content produces no drift (severity: none, lines_changed: 0)",
      "Test 2: 3 line change produces minor drift (severity: minor, lines_changed: 3)",
      "Test 3: 12 line change produces moderate drift (severity: moderate, lines_changed: 12)",
      "Test 4: 30 line change produces major drift (severity: major, lines_changed: 30)",
      "Test 5: computeSoulHash() produces deterministic SHA256 (same content = same hash)",
      "Test 6: diff_snippet contains unified diff output (starts with '@@' line markers)",
      "Test 7: Empty previous content (first checkpoint) produces major drift but not an error (expected for initial checkpoint)",
      "Run: pnpm test soul-drift.test.ts",
      "Verify: all 7 tests pass, severity thresholds match specification (0, 1-4, 5-20, >20)"
    ],
    "passes": true,
    "phase": 1
  },
  {
    "category": "testing",
    "description": "Integration tests for graph CID — verify JSONL Merkle DAG computation",
    "steps": [
      "Create packages/amcp-memory/src/test/graph-cid.test.ts using Vitest",
      "Test 1: computeGraphCID() produces deterministic CID (call twice on same file, verify identical CID)",
      "Test 2: CID format validation (starts with 'bafkrei', is valid CIDv1, uses base32 encoding)",
      "Test 3: Empty file produces valid CID (not null, not empty string)",
      "Test 4: Same content in different files produces same CID (content-addressed property)",
      "Test 5: Single-character change produces different CID (hash sensitivity)",
      "Test 6: Non-existent file returns null (graceful handling)",
      "Test 7: File with invalid JSON line still computes CID (content-addressed regardless of validity — validation is separate concern)",
      "Run: pnpm test graph-cid.test.ts",
      "Verify: all 7 tests pass, CID computation matches IPFS canonical format"
    ],
    "passes": true,
    "phase": 1
  },
  {
    "category": "documentation",
    "description": "Update AMCP-SPEC-v0.2.md — add Layer 1 (Ontology) specification",
    "steps": [
      "Locate AMCP-SPEC-v0.2.md at AMCP-SPEC-v0.2.md (root of repo)",
      "Add new section 'Layer 1: Ontology (Structured Knowledge)' after existing protocol sections (~200 lines)",
      "Document three-layer architecture diagram (ASCII art): Layer 3 (Phenomenological) -> Layer 2 (AMCP) -> Layer 1 (Ontology)",
      "Specify ontology format: append-only JSONL, entity and relation types, schema constraints, temporal awareness (created/updated timestamps)",
      "Document checkpoint metadata extensions: ontologyGraphCID (optional CIDv1), soulHash (optional SHA256), reconstructionSeam (0-7 enum)",
      "Specify validation requirements: schema enforcement, relation integrity, acyclic dependency checking",
      "Document SOUL drift detection protocol: hash comparison, severity classification (minor/moderate/major), notification thresholds",
      "Include reconstruction sequence specification: canonical order (SOUL -> USER -> schema -> graph -> memory -> notes -> tools -> SEAM)",
      "Add examples: sample entity JSON, sample relation JSON, sample checkpoint metadata with ontology fields",
      "Reference academic sources: AriGraph (IJCAI 2025), Zep/Graphiti, A-MEM (NeurIPS 2025), LumenNox phenomenology work",
      "Verify: documentation is technically accurate, protocol version incremented if breaking changes (increment to 0.3 if breaking changes)"
    ],
    "passes": false,
    "phase": 3
  },
  {
    "category": "ontology-evolution",
    "description": "Add evolution tracking to CheckpointMetadata — track memory evolution chains across checkpoints",
    "steps": [
      "Extend CheckpointMetadata interface in packages/amcp-memory/src/checkpoint.ts (after ontologyGraphCID, soulHash, reconstructionSeam from Phase 1)",
      "Add optional fields: evolutionChainCID?: string (CID of evolution.jsonl containing relation inference history), relatedEntitiesCount?: number (count of entities with inferred relations)",
      "Update createCheckpoint() function to accept evolutionChainCID and relatedEntitiesCount in metadata parameter",
      "Add JSDoc: evolutionChainCID points to JSONL log of automatic relation inferences (A-MEM pattern), relatedEntitiesCount is count of entities updated by evolution engine since last checkpoint",
      "Modify serializeCheckpoint() to include new fields in JSON output (backward compatible — omit if undefined)",
      "Verify: TypeScript compilation succeeds, checkpoints with evolution tracking serialize correctly, checkpoints without evolution fields remain valid"
    ],
    "passes": false,
    "phase": 3
  },
  {
    "category": "ontology-evolution",
    "description": "Unit tests for evolution tracking — verify evolution chain integrity",
    "steps": [
      "Create packages/amcp-memory/src/test/evolution-tracking.test.ts using Vitest",
      "Test 1: createCheckpoint() with evolutionChainCID field, verify metadata includes field in serialized output",
      "Test 2: Checkpoint without evolutionChainCID (Phase 1 checkpoint), verify backward compatible (field omitted, not null)",
      "Test 3: relatedEntitiesCount = 5, verify count preserved through serialize/deserialize cycle",
      "Test 4: Invalid CID format for evolutionChainCID rejected with validation error",
      "Test 5: Evolution chain integrity — given checkpoint sequence with evolutionChainCID values, verify CID chain forms valid Merkle DAG (each CID references previous)",
      "Test 6: Zero relatedEntitiesCount valid (no evolution in this checkpoint period)",
      "Run: pnpm test evolution-tracking.test.ts",
      "Verify: all 6 tests pass, evolution metadata persists correctly"
    ],
    "passes": false,
    "phase": 3
  },
  {
    "category": "temporal",
    "description": "Add temporal index utilities — buildTemporalIndex(), queryByTimeRange() for entity version history",
    "steps": [
      "Create packages/amcp-memory/src/temporal-index.ts (<200 lines) with temporal query utilities",
      "Type definitions: TemporalIndexEntry { entity_id: string; checkpoint_cid: string; timestamp: Date; version_hash: string; properties_snapshot: Record<string,any> }, TemporalIndex = Map<string, TemporalIndexEntry[]>",
      "buildTemporalIndex(checkpointCIDs: string[], storage: Storage): Promise<TemporalIndex> — fetches checkpoints, extracts entity snapshots, builds index sorted by timestamp",
      "queryByTimeRange(index: TemporalIndex, entity_id: string, start_date?: Date, end_date?: Date): TemporalIndexEntry[] — filters index by entity and time range",
      "getEntityHistory(index: TemporalIndex, entity_id: string): TemporalIndexEntry[] — returns full timeline for entity (all versions)",
      "Implementation notes: version_hash = SHA256(JSON.stringify(properties)) for change detection, index stores snapshots (not full graph) for memory efficiency",
      "Export from packages/amcp-memory/src/index.ts: export { buildTemporalIndex, queryByTimeRange, getEntityHistory, TemporalIndex, TemporalIndexEntry } from './temporal-index'",
      "Verify: buildTemporalIndex() produces deterministic index, queryByTimeRange() filters correctly, entity with no history returns empty array (not error)"
    ],
    "passes": false,
    "phase": 3
  },
  {
    "category": "temporal",
    "description": "Unit tests for temporal queries — verify version history and time-based filtering",
    "steps": [
      "Create packages/amcp-memory/src/test/temporal-index.test.ts using Vitest",
      "Test 1: Build index from 3 checkpoints, verify index contains entries for all checkpoints sorted chronologically",
      "Test 2: Entity-123 exists in checkpoint-1 and checkpoint-2 with different properties, verify getEntityHistory() returns 2 versions with correct version_hash",
      "Test 3: queryByTimeRange(entity-123, start: 2026-02-10, end: 2026-02-15) returns only versions within range",
      "Test 4: Entity created in checkpoint-3, query range before checkpoint-3, returns empty array",
      "Test 5: Non-existent entity_id, getEntityHistory() returns empty array (graceful handling)",
      "Test 6: Entity properties unchanged across 2 checkpoints, verify version_hash identical (change detection works)",
      "Test 7: buildTemporalIndex() with empty checkpoint list returns empty index (not error)",
      "Test 8: Time zone handling — timestamps with different time zones compared correctly",
      "Run: pnpm test temporal-index.test.ts",
      "Verify: all 8 tests pass, temporal queries accurate, time filtering works across checkpoints"
    ],
    "passes": false,
    "phase": 3
  },
  {
    "category": "contract",
    "description": "Define skill contract types — TypeScript interface for SkillOntologyContract",
    "steps": [
      "Create packages/amcp-core/src/skill-contract.ts (<200 lines) with skill ontology contract types",
      "Type definitions: EntityType = 'Person' | 'Task' | 'Project' | 'Goal' | 'Event' | ... (comprehensive enum), RelationType = 'has_owner' | 'blocks' | 'depends_on' | ...",
      "SkillOntologyContract interface: { skill_name: string; version: string; ontology: { reads: EntityType[]; writes: EntityType[]; preconditions: ContractPredicate[]; postconditions: ContractPredicate[] } }",
      "ContractPredicate type: { entity_type: EntityType; condition: string; description?: string } (e.g., { entity_type: 'Task', condition: 'assignee exists', description: 'All tasks must have an assignee' })",
      "validateContract(contract: SkillOntologyContract): ValidationResult — validates contract structure (required fields, valid entity types)",
      "checkPreconditions(contract: SkillOntologyContract, graph: Entity[]): CheckResult — evaluates preconditions against current graph state",
      "detectConflicts(contracts: SkillOntologyContract[]): ConflictResult[] — finds incompatible postconditions across contracts",
      "Export from packages/amcp-core/src/index.ts: export types and validator functions",
      "Verify: TypeScript strict mode passes, contract types are self-documenting with JSDoc comments"
    ],
    "passes": false,
    "phase": 3
  },
  {
    "category": "contract",
    "description": "Unit tests for contract validation — verify precondition and postcondition checking",
    "steps": [
      "Create packages/amcp-core/src/test/skill-contract.test.ts using Vitest",
      "Test 1: Valid contract with all required fields passes validateContract()",
      "Test 2: Contract missing 'skill_name' field fails validation with descriptive error",
      "Test 3: Invalid EntityType in reads array fails validation",
      "Test 4: checkPreconditions() with condition 'Task.assignee exists', all Tasks have assignee, returns success",
      "Test 5: checkPreconditions() with condition 'Task.assignee exists', some Tasks missing assignee, returns failure with list of violating entities",
      "Test 6: detectConflicts() with two contracts having incompatible postconditions (skill-A: 'Task.status = done', skill-B: 'Task.status != done'), returns conflict",
      "Test 7: Compatible contracts (skill-A writes Task, skill-B reads Task), detectConflicts() returns empty array",
      "Test 8: Empty preconditions array valid (skill has no preconditions)",
      "Test 9: Condition syntax validation — reject invalid condition strings (e.g., 'Task.assignee = = exists')",
      "Run: pnpm test skill-contract.test.ts",
      "Verify: all 9 tests pass, contract validation catches malformed contracts, precondition evaluation works"
    ],
    "passes": false,
    "phase": 3
  },
  {
    "category": "learning",
    "description": "Extend CheckpointMetadata interface — add unresolvedProblems, recentLearnings, learningStats fields per protocol-09",
    "steps": [
      "Locate CheckpointMetadata interface in packages/amcp-memory/src/checkpoint.ts",
      "Add optional fields: unresolvedProblems?: Problem[] (problems with status != solved at checkpoint time), recentLearnings?: Learning[] (learnings acquired since last checkpoint), learningStats?: LearningStats",
      "LearningStats type: { totalProblems: number; totalSolved: number; solvedPostRecovery: number; totalLearnings: number }",
      "Update createCheckpoint() function to accept these fields in metadata parameter",
      "Add JSDoc comments: unresolvedProblems is serialized from memory/learning/problems.jsonl filtered by status, recentLearnings is delta since previous checkpoint, solvedPostRecovery tracks self-improvement metric",
      "Verify: TypeScript compilation succeeds, existing checkpoints without learning fields remain valid (backward compatible)"
    ],
    "passes": false,
    "phase": 2
  },
  {
    "category": "learning",
    "description": "Define Problem and Learning entity types — TypeScript types per protocol-09-learning-schema",
    "steps": [
      "Create packages/amcp-memory/src/learning-types.ts (<150 lines) with Problem and Learning type definitions",
      "ProblemStatus enum: open, stuck, solved, abandoned",
      "Problem type: { id: string; description: string; status: ProblemStatus; blocker?: string; attempts: number; lastApproach?: string; retryAfter?: Date; created: Date; updated: Date; resolved?: Date; humanVerified: boolean; source: 'command' | 'skill' | 'self-detect' }",
      "LearningConfidence enum: tentative, verified",
      "Learning type: { id: string; insight: string; sourceProblem?: string; confidence: LearningConfidence; humanVerified: boolean; verifiedAt?: Date; tags: string[]; created: Date }",
      "ID format validators: isProblemId(id: string): boolean (matches prob_{uuid8}), isLearningId(id: string): boolean (matches learn_{uuid8})",
      "Export all types from packages/amcp-memory/src/index.ts",
      "Verify: TypeScript strict mode passes, types match protocol-09-learning-schema.json specification"
    ],
    "passes": false,
    "phase": 2
  },
  {
    "category": "learning",
    "description": "Add validateLearningData() function — validate Problem and Learning JSONL files",
    "steps": [
      "Create packages/amcp-memory/src/validate-learning.ts (<150 lines) with validateLearningData(dirPath: string): Promise<ValidationResult>",
      "ValidationResult type: { valid: boolean; problemCount: number; learningCount: number; errors: string[] }",
      "Validation checks: 1) problems.jsonl exists and each line is valid JSON matching Problem schema, 2) learnings.jsonl exists and each line is valid JSON matching Learning schema, 3) stats.json exists and has required fields",
      "Cross-reference validation: Learning.sourceProblem (if set) references existing Problem ID",
      "Status consistency: stats.totalProblems matches actual problem count, stats.totalSolved matches count of status=solved",
      "Return detailed errors: 'problems.jsonl line 15: missing required field description', 'learnings.jsonl line 8: sourceProblem prob_abc123 not found'",
      "Export from packages/amcp-memory/src/index.ts",
      "Verify: validateLearningData() rejects invalid data, accepts valid data, cross-references are enforced"
    ],
    "passes": false,
    "phase": 2
  },
  {
    "category": "learning",
    "description": "Add computeLearningStats() utility — aggregate statistics from JSONL files",
    "steps": [
      "Add computeLearningStats(dirPath: string): Promise<LearningStats> to packages/amcp-memory/src/validate-learning.ts",
      "Parse problems.jsonl, count by status: open, stuck, solved, abandoned",
      "Parse learnings.jsonl, count by confidence: tentative, verified",
      "Compute solvedPostRecovery: count problems where resolved timestamp > agent's last resurrection timestamp (passed as parameter or read from checkpoint metadata)",
      "Return LearningStats: { totalProblems, totalSolved, solvedPostRecovery, totalLearnings, problemsByStatus, learningsByConfidence }",
      "Handle missing files gracefully: return zeros if memory/learning/ doesn't exist",
      "Export from packages/amcp-memory/src/index.ts",
      "Verify: stats computation matches manual count, handles empty/missing files"
    ],
    "passes": false,
    "phase": 2
  },
  {
    "category": "testing",
    "description": "Unit tests for learning types — verify Problem and Learning schema validation",
    "steps": [
      "Create packages/amcp-memory/src/test/learning-types.test.ts using Vitest",
      "Test 1: Valid Problem object passes type check, all required fields present",
      "Test 2: Problem missing 'description' field fails validation",
      "Test 3: Problem with invalid status value fails validation",
      "Test 4: Valid Learning object passes type check",
      "Test 5: Learning with invalid confidence value fails validation",
      "Test 6: isProblemId('prob_abc12345') returns true, isProblemId('invalid') returns false",
      "Test 7: isLearningId('learn_xyz98765') returns true, isLearningId('prob_abc12345') returns false",
      "Run: pnpm test learning-types.test.ts",
      "Verify: all 7 tests pass, type validation catches schema violations"
    ],
    "passes": true,
    "phase": 1
  },
  {
    "category": "testing",
    "description": "Unit tests for learning validation — verify JSONL parsing and cross-reference checks",
    "steps": [
      "Create packages/amcp-memory/src/test/validate-learning.test.ts using Vitest",
      "Test 1: Valid memory/learning/ directory with problems.jsonl, learnings.jsonl, stats.json passes validation",
      "Test 2: Invalid JSON on line 3 of problems.jsonl fails with parse error",
      "Test 3: Learning references non-existent Problem ID fails with cross-reference error",
      "Test 4: stats.json totalProblems doesn't match actual count fails with consistency error",
      "Test 5: Empty directory (no files) returns valid=true with zero counts (graceful degradation)",
      "Test 6: Missing problems.jsonl but learnings.jsonl exists, returns partial validation (problems count=0)",
      "Test 7: computeLearningStats() matches expected counts for test fixture",
      "Run: pnpm test validate-learning.test.ts",
      "Verify: all 7 tests pass, validation catches all specified error types"
    ],
    "passes": true,
    "phase": 1
  },
  {
    "category": "testing",
    "description": "Integration tests for learning checkpoint — verify learning data roundtrip through checkpoint/restore",
    "steps": [
      "Create packages/amcp-memory/src/test/learning-checkpoint.test.ts using Vitest",
      "Test 1: Create checkpoint with unresolvedProblems array, verify metadata includes serialized problems",
      "Test 2: Create checkpoint with recentLearnings array, verify metadata includes serialized learnings",
      "Test 3: Create checkpoint with learningStats, verify stats preserved through serialize/deserialize",
      "Test 4: Checkpoint without learning fields (backward compatible), verify no errors",
      "Test 5: Restore checkpoint with learning data, verify memory/learning/ directory recreated correctly",
      "Test 6: solvedPostRecovery metric: create problem, checkpoint, restore, solve problem, checkpoint again, verify solvedPostRecovery incremented",
      "Run: pnpm test learning-checkpoint.test.ts",
      "Verify: all 6 tests pass, learning data survives checkpoint/restore cycle"
    ],
    "passes": false,
    "phase": 1
  },
  {
    "category": "documentation",
    "description": "Update AMCP-SPEC — add Learning Layer specification per protocol-09",
    "steps": [
      "Update AMCP-SPEC-v0.2.md or create AMCP-SPEC-v0.3.md in same location",
      "Document Problem entity schema: id format (prob_{uuid8}), required/optional fields, status enum, source tracking",
      "Document Learning entity schema: id format (learn_{uuid8}), confidence levels, human verification requirement",
      "Document checkpoint metadata extensions: unresolvedProblems, recentLearnings, learningStats",
      "Specify validation requirements: schema enforcement, cross-reference integrity, stats consistency",
      "Document self-improvement metric: solvedPostRecovery / totalSolved = capability growth rate",
      "Include examples: sample Problem JSON, sample Learning JSON, sample checkpoint metadata with learning fields",
      "Reference protocol-09-learning-schema.json as canonical schema definition",
      "Document trigger types: command prefix (/remember, /stuck, /learned), SKILL matching, self-detect",
      "Verify: documentation matches TypeScript types, all fields documented with JSDoc-style descriptions"
    ],
    "passes": false,
    "phase": 3
  },
  {
    "category": "pinning",
    "phase": 1,
    "description": "Add PinningProvider interface to @amcp/core — abstraction for IPFS pinning services",
    "steps": [
      "Create packages/amcp-core/src/pinning.ts with PinningProvider interface",
      "Interface methods: pin(cid: string, name?: string): Promise<PinResult>, unpin(cid: string): Promise<void>, status(cid: string): Promise<PinStatus>",
      "PinResult type: { cid: string, requestId: string, status: PinStatus }",
      "PinStatus enum: Queued, Pinning, Pinned, Failed",
      "Add PinataProvider class implementing PinningProvider (existing behavior)",
      "Add SolvrProvider class implementing PinningProvider (new)",
      "SolvrProvider calls: POST /v1/pins, GET /v1/pins/:id, DELETE /v1/pins/:id",
      "Export from packages/amcp-core/src/index.ts",
      "Verify: both providers compile and implement same interface"
    ],
    "passes": true
  },
  {
    "category": "pinning",
    "phase": 1,
    "description": "Implement SolvrProvider — Solvr pinning API client in TypeScript",
    "steps": [
      "Create packages/amcp-core/src/providers/solvr.ts with SolvrProvider class",
      "Constructor: accepts { apiKey: string, baseUrl?: string } (default: https://api.solvr.dev)",
      "pin() method: POST /v1/pins with { cid, name }, poll for status=pinned",
      "unpin() method: DELETE /v1/pins/:requestId",
      "status() method: GET /v1/pins/:requestId, parse status field",
      "Add timeout handling: max 5 minutes for pin completion",
      "Add retry logic: exponential backoff on 429/5xx errors",
      "Add error types: SolvrPinError, SolvrAuthError, SolvrTimeoutError",
      "Verify: SolvrProvider can pin CID to Solvr API"
    ],
    "passes": true
  },
  {
    "category": "pinning",
    "phase": 1,
    "description": "Update createCheckpoint() to accept PinningProvider — decouple from Pinata",
    "steps": [
      "Locate createCheckpoint() in packages/amcp-memory/src/checkpoint.ts",
      "Add optional pinningProvider parameter: pinningProvider?: PinningProvider",
      "If provided: use pinningProvider.pin() instead of hardcoded Pinata call",
      "If not provided: fall back to PinataProvider (backward compatible)",
      "Update return type to include providerUsed: 'pinata' | 'solvr' | string",
      "Add config option to specify default provider",
      "Verify: createCheckpoint() works with both Pinata and Solvr providers"
    ],
    "passes": true
  },
  {
    "category": "pinning",
    "phase": 2,
    "description": "Add multi-provider support — pin to multiple services for redundancy",
    "steps": [
      "Create packages/amcp-core/src/providers/multi.ts with MultiProvider class",
      "Constructor: accepts PinningProvider[] array",
      "pin() method: calls all providers in parallel, returns first success",
      "Option: requireAll (all must succeed) vs requireOne (any success is OK)",
      "Track results: { provider: string, success: boolean, cid?: string, error?: string }[]",
      "Return combined result with all provider statuses",
      "Add to checkpoint metadata: providers: { name: string, cid: string }[]",
      "Verify: MultiProvider pins to both Pinata and Solvr simultaneously"
    ],
    "passes": false
  },
  {
    "category": "verification",
    "phase": 2,
    "description": "Add checkpoint verification via Solvr — cross-check CID availability",
    "steps": [
      "Create packages/amcp-core/src/verify.ts with verifyCheckpoint() function",
      "Accept: cid, providers (array of PinningProvider)",
      "Check each provider: status(cid) returns Pinned",
      "Compare content: fetch from multiple gateways, verify hashes match",
      "Return VerificationResult: { verified: boolean, providers: { name: string, available: boolean }[], contentMatch: boolean }",
      "Add to resuscitate flow: verify checkpoint before restore",
      "Verify: checkpoint pinned to both Pinata and Solvr passes verification"
    ],
    "passes": false
  },
  {
    "category": "metadata",
    "phase": 2,
    "description": "Extend CheckpointMetadata with pinning provider information",
    "steps": [
      "Locate CheckpointMetadata interface in packages/amcp-memory/src/checkpoint.ts",
      "Add optional field: pinningProviders?: { name: string, cid: string, requestId?: string }[]",
      "Add optional field: primaryProvider?: string (which provider to use for retrieval)",
      "Update createCheckpoint() to populate these fields",
      "Update verifyCheckpoint() to use provider info for verification",
      "Backward compatible: old checkpoints without these fields still work",
      "Verify: checkpoint metadata includes provider information"
    ],
    "passes": false
  },
  {
    "category": "testing",
    "phase": 1,
    "description": "Unit tests for PinningProvider implementations — Pinata and Solvr",
    "steps": [
      "Create packages/amcp-core/src/test/pinning.test.ts using Vitest",
      "Test 1: PinataProvider.pin() returns valid CID (mock HTTP)",
      "Test 2: SolvrProvider.pin() returns valid CID (mock HTTP)",
      "Test 3: SolvrProvider handles rate limiting with retry",
      "Test 4: SolvrProvider times out after 5 minutes",
      "Test 5: Both providers implement same interface (type check)",
      "Test 6: MultiProvider pins to all providers",
      "Test 7: MultiProvider handles partial failure (one succeeds, one fails)",
      "Run: pnpm test pinning.test.ts",
      "Verify: all 7 tests pass"
    ],
    "passes": true
  },
  {
    "category": "documentation",
    "phase": 3,
    "description": "Update AMCP-SPEC with pinning provider abstraction",
    "steps": [
      "Add 'Pinning Providers' section to AMCP-SPEC",
      "Document PinningProvider interface contract",
      "Document built-in providers: Pinata, Solvr",
      "Document multi-provider redundancy pattern",
      "Document checkpoint verification flow",
      "Include code examples for custom provider implementation",
      "Verify: documentation matches implementation"
    ],
    "passes": false
  },
  {
    "category": "solvr-integration",
    "phase": 1,
    "description": "Add Solvr IPFS server details to AMCP config — solvr-ipfs-01 connection info",
    "steps": [
      "Document live Solvr IPFS server in DEPLOYMENT.md: solvr-ipfs-01 @ 65.109.134.87",
      "Add to config schema: storage.solvr.ipfsServer: { ip, peer_id, gateway_url }",
      "Default gateway: https://ipfs.solvr.dev/ipfs/ (will proxy to solvr-ipfs-01)",
      "Peer ID for direct connections: 12D3KooWJG6rZ1KWTQy1fPeaZuxhfukik3RmYTjyf76Yn6CwUP3A",
      "Verify: AMCP CLI can connect to Solvr IPFS for checkpoint retrieval"
    ],
    "passes": true,
    "server_info": {
      "name": "solvr-ipfs-01",
      "ip": "65.109.134.87",
      "peer_id": "12D3KooWJG6rZ1KWTQy1fPeaZuxhfukik3RmYTjyf76Yn6CwUP3A",
      "kubo_version": "0.39.0",
      "deployed": "2026-02-17"
    }
  }
]
