# amcp-protocol Progress

## 2026-02-14: Identity schema validation (prd-v1 task 1)
- Created `packages/amcp-core/src/validate-identity.ts` — `validateIdentitySchema()` function
  - Validates flat format: requires aid (string), publicKey (string), privateKey (string)
  - Validates old agent format: requires agent.aid, agent.currentPublicKey, agent.currentPrivateKey
  - Rejects non-KERI AIDs (must start with Ed25519 prefix "B")
  - Rejects openclaw-deploy-style identities (sha256 AID, no KERI prefix)
  - Collects all errors (doesn't fail on first)
- Integrated into `loadIdentity()` in `scripts/amcp-cli.ts` (line 111)
  - Calls `validateIdentitySchema()` before format detection
  - Throws descriptive error on invalid identity with all collected errors
- Exported `validateIdentitySchema` and `IdentityValidationResult` from `@amcp/core` index
- Exported `isValidAid` from `@amcp/core` index (was missing)
- Added 17 test cases in `packages/amcp-core/src/test/validate-identity.test.ts`
- All 198 tests pass, build succeeds

## 2026-02-14: Ed25519 on-curve validation for isValidAid() (prd-v1 task 2)
- Updated `isValidAid()` in `packages/amcp-core/src/aid.ts`
  - Added `Point.fromBytes(publicKey, false)` — rejects bytes not on Ed25519 curve
  - Added `clearCofactor().equals(Point.ZERO)` — rejects small-order torsion points
  - Imported `Point` from `@noble/ed25519` (existing dependency)
- `isValidAid` was already exported from `@amcp/core` index.ts (task 1 did this)
- Added 11 test cases in `packages/amcp-core/src/test/aid-validation.test.ts`
  - Rejection: invalid curve points (y=2, y=7), torsion (all-zeros), 0xFF fill, wrong prefix, wrong length, empty, prefix-only, sha256 hex AID
  - Acceptance: real Ed25519 keypair AID, multiple generated AIDs
- All 209 tests pass, build succeeds

## 2026-02-14: 'amcp identity validate' CLI command (prd-v1 task 3)
- Created `validateIdentityFull()` in `packages/amcp-core/src/validate-identity.ts`
  - Composes all validation layers: schema check, AID Ed25519 on-curve check, AID-publicKey consistency, KEL integrity
  - Async function (KEL verification requires async crypto)
  - Returns `{ valid: boolean, errors: string[] }` with all collected errors
  - Skips KEL check when no KEL is present (flat CLI identities don't have KEL)
- Added `identity validate` subcommand to `scripts/amcp-cli.ts`
  - Accepts `--path` flag (defaults to `~/.amcp/identity.json`)
  - Outputs structured JSON: `{ valid: true }` or `{ valid: false, errors: [...] }`
  - Exit code 0 on valid, 1 on invalid — designed for scripting use
- Exported `validateIdentityFull` from `@amcp/core` index
- Added 12 test cases in `packages/amcp-core/src/test/validate-identity-full.test.ts`
  - Rejection: empty object, openclaw-deploy fake, invalid curve point, 0xFF fill, AID-publicKey mismatch, empty KEL, KEL AID mismatch
  - Acceptance: valid flat identity, valid with KEL, valid old-format, no KEL skips check, multi-error collection
- All 221 tests pass, build succeeds

## 2026-02-14: KEL integrity check in loadIdentity() (prd-v1 task 4)
- Upgraded `loadIdentity()` in `scripts/amcp-cli.ts` to use `validateIdentityFull()` instead of `validateIdentitySchema()`
  - Now runs all 4 validation layers on load: schema, AID crypto, AID-publicKey consistency, KEL integrity
  - Calls `verifyKEL()` via `validateIdentityFull()` — rejects corrupt/missing inception events
  - Made `loadIdentity()` async (required for `verifyKEL()` async crypto)
  - Updated all 3 call sites to `await loadIdentity()` (identity show, checkpoint create, resuscitate)
  - Removed unused `validateIdentitySchema` import (superseded by `validateIdentityFull`)
- Added 9 test cases in `packages/amcp-core/src/test/load-identity-kel.test.ts`
  - Rejection: empty KEL, missing inception (starts with rotation), wrong inception sn, corrupted signature, tampered AID, KEL AID mismatch
  - Acceptance: valid KEL from createAgent, identity without KEL, old-format with valid KEL
- All 230 tests pass, build succeeds

## 2026-02-14: Export validateIdentityFile(path) from @amcp/core (prd-v1 task 5)
- Created `packages/amcp-core/src/validate.ts` — `validateIdentityFile(path)` function
  - Reads identity file from disk using `node:fs/promises`
  - Parses JSON with descriptive error on invalid syntax
  - Validates JSON is a non-null object (rejects arrays, null, primitives)
  - Delegates to `validateIdentityFull()` for full validation pipeline
  - Returns `{ valid: boolean, errors: string[] }` — never throws
- Exported `validateIdentityFile` from `@amcp/core` index.ts
- Added 12 test cases in `packages/amcp-core/src/test/validate-identity-file.test.ts`
  - File-level: nonexistent file, invalid JSON, JSON array, JSON null
  - Identity-level: empty object, openclaw-deploy fake, invalid AID, AID-publicKey mismatch, corrupt KEL
  - Acceptance: valid flat identity, valid with KEL, valid old-format
  - Uses real temp files on disk (no mocks)
- All 242 tests pass, build succeeds

## 2026-02-14: Fake identity rejection test suite (prd-v1 task 6)
- Created `packages/amcp-core/src/test/validate.test.ts` — unified end-to-end tests
  - Uses canonical openclaw-deploy-style fake identity fixture (sha256 hex AID, secrets.pinata_jwt, no publicKey/privateKey, no KEL)
  - Verifies fake identity is rejected by EVERY validation layer:
    - `validateIdentitySchema()` — rejects with missing publicKey, privateKey, and non-KERI AID errors
    - `isValidAid()` — rejects sha256 hex AID (no KERI prefix) and 0xFF bytes with B prefix (not on Ed25519 curve)
    - `validateIdentityFull()` — returns valid:false with schema + KERI prefix errors
    - `validateIdentityFile()` — rejects from disk with multiple errors, including disguised B-prefix attack
  - Positive tests: valid KERI identity (from createAgent()) passes all 4 layers
- Added 14 test cases covering all validation layers end-to-end
- All 256 tests pass, build succeeds

## 2026-02-18: PinningProvider interface for @amcp/core (prd-v1 pinning task 1)
- Created `packages/amcp-core/src/pinning.ts` — PinningProvider abstraction for IPFS pinning services
  - `PinningProvider` interface: `pin(cid, name?)`, `unpin(cid)`, `status(cid)`, `name` readonly property
  - `PinStatus` enum: Queued, Pinning, Pinned, Failed
  - `PinResult` type: `{ cid, requestId, status }`
  - `PinningProviderConfig` type: `{ apiKey, baseUrl? }`
  - `PinataProvider` class: implements PinningProvider for Pinata API (pin by hash, unpin, job status)
  - `SolvrProvider` class: implements PinningProvider for Solvr API (POST/DELETE/GET /v1/pins)
  - Error types: `PinataError`, `SolvrPinError`, `SolvrAuthError`, `SolvrTimeoutError`
  - Both providers validate API key on construction (reject empty)
  - Both accept configurable baseUrl with sensible defaults
- Exported all types, classes, and enums from `@amcp/core` index.ts
- Added 16 test cases in `packages/amcp-core/src/test/pinning.test.ts`
  - PinStatus enum values, interface compliance for both providers
  - Constructor validation (baseUrl defaults, custom baseUrl, empty API key rejection)
  - Method existence and async behavior (pin, unpin, status)
  - Type compatibility: both providers assignable to PinningProvider[]
- All 272 tests pass, build succeeds

## 2026-02-18: SolvrProvider implementation with retry/timeout (prd-v1 pinning task 2)
- Extended `SolvrProvider` in `packages/amcp-core/src/pinning.ts`
  - `SolvrProviderConfig` extends `PinningProviderConfig` with `timeoutMs?` (default 5min) and `maxRetries?` (default 3)
  - `fetchWithRetry()` private method: exponential backoff on 429/5xx errors
  - Abort-signal-based timeout: wraps fetch with AbortController, throws `SolvrTimeoutError` on abort
  - Retry delays: 1s * 2^attempt (1s, 2s, 4s)
- Added 5 tests for retry/timeout config in `packages/amcp-core/src/test/pinning.test.ts`
  - Default timeout (5 min), custom timeout, default maxRetries (3), custom maxRetries
  - SolvrTimeoutError construction and instanceof check
- All 277 tests pass, build succeeds

## 2026-02-18: PinningProvider test suite (prd-v1 testing/pinning task)
- 21 tests total in `packages/amcp-core/src/test/pinning.test.ts`
  - PinStatus enum, interface compliance, constructor config, method existence
  - Both providers assignable to PinningProvider[], retry/timeout config, error types
- All 277 tests pass, build succeeds

## 2026-02-18: Update createCheckpoint() to accept PinningProvider (prd-v1 pinning task 3)
- Updated `createCheckpoint()` in `packages/amcp-memory/src/checkpoint.ts`
  - Added optional 5th parameter: `options?: CreateCheckpointOptions`
  - `CreateCheckpointOptions` interface: `{ pinningProvider?: PinningProvider }`
  - `CreateCheckpointResult` interface: adds `providerUsed?: string` to return type
  - If provider given: calls `provider.pin(contentCid, name)` after signing, sets `providerUsed`
  - If no provider: backward compatible, `providerUsed` is undefined
- Exported `CreateCheckpointOptions`, `CreateCheckpointResult` from `@amcp/memory` index
- Added 6 test cases in `packages/amcp-memory/test/checkpoint-pinning.test.ts`
  - Backward compatibility (no provider), provider integration, CID pinning verification
  - Provider name tracking, signature validity with/without provider, prior chain support
- All 320 tests pass (277 amcp-core + 43 amcp-memory), both packages build successfully

## 2026-02-18: Solvr IPFS server config (prd-v1 solvr-integration task)
- Added Solvr types to `packages/amcp-memory/src/storage/interface.ts`
  - `SolvrIpfsServerConfig` interface: `{ ip, peerId, gatewayUrl }`
  - `SolvrConfig` interface: `{ ipfsServer: SolvrIpfsServerConfig }`
  - Extended `StorageConfig` with optional `solvr?: SolvrConfig`
- Added `https://ipfs.solvr.dev/ipfs/` to default gateways in `IPFSBackend` (2nd priority after Pinata)
- Exported `SolvrConfig`, `SolvrIpfsServerConfig` from storage index
- Added 4 tests in `packages/amcp-memory/test/solvr-config.test.ts`
- All 324 tests pass (277 amcp-core + 47 amcp-memory), both packages build successfully

## 2026-02-18: SOUL drift detection utilities (prd-v1 testing task 13 + ontology task 11)
- Created `packages/amcp-core/src/soul-drift.ts` — SOUL identity change detection
  - `computeSoulHash(content)`: SHA-256 hash using @noble/hashes/sha256
  - `detectSoulDrift(prevHash, currHash, prevContent, currContent)`: DriftResult
  - `DriftResult` type: `{ drifted, severity, lines_changed, diff_snippet? }`
  - `DriftSeverity` type: 'none' | 'minor' | 'moderate' | 'major'
  - Positional line diff algorithm: changed line at same position = 1 change
  - Severity thresholds: 0=none, 1-4=minor, 5-20=moderate, >20=major
- Exported `computeSoulHash`, `detectSoulDrift`, `DriftResult`, `DriftSeverity` from `@amcp/core` index
- Added 10 test cases in `packages/amcp-core/src/test/soul-drift.test.ts`
  - Hash determinism, different content, empty string, no drift, minor/moderate/major severity
  - Diff snippet presence, empty previous (initial checkpoint), no snippet when no drift
- All 334 tests pass (287 amcp-core + 47 amcp-memory), both packages build successfully

## 2026-02-18: Graph CID computation (prd-v1 testing task 14 + ontology task 8)
- Created `packages/amcp-memory/src/graph-cid.ts` — `computeGraphCID(filePath)`
  - Reads JSONL file, computes CIDv1 using existing `computeCID()` from cid.ts
  - Returns null for non-existent files (graceful handling)
  - Content-addressed: same content = same CID regardless of filename
- Exported `computeGraphCID` from `@amcp/memory` index
- Added 7 tests in `packages/amcp-memory/test/graph-cid.test.ts`
  - Deterministic CID, format validation, empty file, same content, hash sensitivity, non-existent file, invalid JSON

## 2026-02-18: Ontology validation (prd-v1 testing task 12 + ontology task 10)
- Created `packages/amcp-memory/src/validate-ontology.ts` — `validateOntologySchema(filePath)`
  - `OntologyValidationResult`: `{ valid, entity_count, relation_count, errors[] }`
  - Entity validation: requires id, entityType, properties, created, updated
  - Relation validation: requires from_id, relation_type, to_id
  - Relation integrity: from_id and to_id must reference existing entity IDs
  - Cycle detection: DFS on 'blocks' and 'depends_on' relations, detects circular dependencies
  - Detailed error messages: "Line N: ..." format
- Exported `validateOntologySchema`, `OntologyValidationResult` from `@amcp/memory` index
- Added 7 tests in `packages/amcp-memory/test/validate-ontology.test.ts`
  - Valid graph, invalid JSON, missing fields, non-existent references, cycles, empty file, mixed lines
- All 348 tests pass (287 amcp-core + 61 amcp-memory), both packages build successfully

## 2026-02-18: Learning types — Problem and Learning entities (prd-v1 task 26)
- Created `packages/amcp-memory/src/learning-types.ts` — Problem and Learning type definitions
  - `ProblemStatus` enum: Open, Stuck, Solved, Abandoned
  - `Problem` interface: id, description, status, blocker?, attempts, lastApproach?, retryAfter?, created, updated, resolved?, humanVerified, source, tags
  - `LearningConfidence` enum: Tentative, Verified
  - `Learning` interface: id, insight, sourceProblem?, confidence, humanVerified, verifiedAt?, tags, created
  - `isProblemId()`: matches `prob_` + 8 lowercase alphanumeric chars
  - `isLearningId()`: matches `learn_` + 8 lowercase alphanumeric chars
  - `validateProblem()`: validates all required fields, enum values, id format
  - `validateLearning()`: validates all required fields, enum values, id format
- Exported all types, enums, functions from `@amcp/memory` index.ts
- Added 16 test cases in `packages/amcp-memory/test/learning-types.test.ts`
  - ProblemStatus enum values, LearningConfidence enum values
  - Valid Problem passes, missing description fails, invalid status fails, invalid id fails, invalid source fails
  - Valid Learning passes, invalid confidence fails, missing insight fails
  - isProblemId: accepts valid, rejects invalid/wrong prefix/too short/too long/empty
  - isLearningId: accepts valid, rejects invalid/wrong prefix/too short/too long/empty
- All 364 tests pass (287 amcp-core + 77 amcp-memory), both packages build successfully

## 2026-02-18: Learning validation — validateLearningData() and computeLearningStats() (prd-v1 task 27)
- Created `packages/amcp-memory/src/validate-learning.ts` — JSONL validation and statistics
  - `validateLearningData(dirPath)`: validates problems.jsonl, learnings.jsonl, stats.json
    - Parses each JSONL file line-by-line, validates required fields
    - Cross-reference: Learning.sourceProblem must reference existing Problem ID
    - Stats consistency: stats.json totalProblems and totalSolved must match actual counts
    - Graceful handling: missing files → zero counts, not errors
    - Returns `LearningDataValidationResult { valid, problemCount, learningCount, errors[] }`
  - `computeLearningStats(dirPath)`: aggregates statistics from JSONL files
    - Counts problems by status (open, stuck, solved, abandoned)
    - Counts learnings by confidence (tentative, verified)
    - Returns `LearningStats { totalProblems, totalSolved, solvedPostRecovery, totalLearnings, problemsByStatus, learningsByConfidence }`
    - Handles missing files gracefully (returns zeros)
- Exported `validateLearningData`, `computeLearningStats`, `LearningDataValidationResult`, `LearningStats` from `@amcp/memory` index
- Added 8 test cases in `packages/amcp-memory/test/validate-learning.test.ts`
  - Valid directory passes, invalid JSON on line 3 fails, cross-reference to non-existent problem fails
  - Stats totalProblems mismatch fails, empty directory returns valid with zeros
  - Missing problems.jsonl with learnings.jsonl returns partial validation
  - computeLearningStats matches expected counts, returns zeros for empty directory
- All 414 tests pass (287 amcp-core + 85 amcp-memory + 26 recovery + 16 exchange), build succeeds

## 2026-02-18: Learning checkpoint integration tests (prd-v1 task 28)
- Created `packages/amcp-memory/test/learning-checkpoint.test.ts` — learning data roundtrip through checkpoint
  - Test 1: Checkpoint with unresolvedProblems array — serialized problems in metadata.custom survive JSON roundtrip
  - Test 2: Checkpoint with recentLearnings array — serialized learnings in metadata.custom survive JSON roundtrip
  - Test 3: Checkpoint with learningStats — stats preserved through serialize/deserialize (totalProblems, totalSolved, solvedPostRecovery, problemsByStatus, learningsByConfidence)
  - Test 4: Checkpoint without learning fields — backward compatible, no errors, custom is undefined
  - Test 5: Restore checkpoint — write learning data from checkpoint metadata to new directory, verify file content matches original, stats match
  - Test 6: solvedPostRecovery metric — create problem, checkpoint, restore, solve problem, checkpoint again, verify solvedPostRecovery=1
- Uses real createAgent(), createCheckpoint(), computeLearningStats() — no mocks
- Learning data stored in CheckpointMetadata.custom field (existing extensibility point)
- All 420 tests pass (287 amcp-core + 91 amcp-memory + 26 recovery + 16 exchange), build succeeds

## 2026-02-18: Extend CheckpointMetadata with ontology fields (prd-v1 task 7, Phase 2)
- Extended `CheckpointMetadata` interface in `packages/amcp-memory/src/checkpoint.ts`
  - Added `ontologyGraphCID?: string` — CIDv1 of memory/ontology/graph.jsonl (omit if no ontology)
  - Added `soulHash?: string` — SHA-256 hash of SOUL.md content for drift detection
  - Added `reconstructionSeam?: number` — reconstruction step (0-7) when identity coalesced (default: 7 = post-context-load)
  - All fields optional — backward compatible with existing checkpoints
  - JSDoc comments on each field explaining purpose
- Added 11 test cases in `packages/amcp-memory/test/checkpoint-ontology.test.ts`
  - ontologyGraphCID: accepted in metadata, omitted when not provided
  - soulHash: accepted in metadata, omitted when not provided
  - reconstructionSeam: accepted values 0 and 7, omitted when not provided
  - All three fields together: accepted simultaneously, survive JSON roundtrip
  - Signature validity: checkpoints with ontology metadata verify correctly
  - Backward compatibility: existing checkpoints without ontology fields remain valid
- All 431 tests pass (287 amcp-core + 102 amcp-memory + 26 recovery + 16 exchange), build succeeds

## 2026-02-18: Define ontology entity types (prd-v1 task 9, Phase 2)
- Created `packages/amcp-memory/src/ontology-types.ts` — typed graph entity and relation definitions
  - `EntityType` enum: Person, Organization, Project, Task, Goal, Event, Location, Document, Message, Note, Account, Credential, Action, Policy (14 types)
  - `RelationType` enum: has_owner, owns, has_task, part_of, member_of, blocks, depends_on, mentions, references (9 types)
  - `isValidEntityType()` and `isValidRelationType()` type guard functions
  - `Entity` interface: id, entityType, properties, created, updated
  - `Relation` interface: from_id, relation_type, to_id, properties? (optional)
  - `PersonEntity` concrete type: extends Entity with typed name, email?, phone?, notes?
  - `EntityConstraint` interface: type, required_properties, forbidden_properties?, acyclic_relations?
- Exported all types, enums, and functions from `@amcp/memory` index.ts
- Added 18 test cases in `packages/amcp-memory/test/ontology-types.test.ts`
  - EntityType: all 14 values, count validation
  - RelationType: all 9 values, count validation
  - isValidEntityType: accepts valid, rejects invalid/empty/wrong-case
  - isValidRelationType: accepts valid, rejects invalid/empty/wrong-case
  - Entity: valid object, arbitrary properties
  - Relation: valid object, optional properties, undefined when omitted
  - PersonEntity: fixed entityType, optional fields
  - EntityConstraint: required, forbidden, acyclic relations
- All 449 tests pass (287 amcp-core + 120 amcp-memory + 26 recovery + 16 exchange), build succeeds

## 2026-02-18: Reconstruction sequence utilities (prd-v1 task 11, Phase 2)
- Created `packages/amcp-recovery/src/reconstruction.ts` — canonical memory loading order
  - `ReconstructionStep` enum: SOUL=0, USER=1, ONTOLOGY_SCHEMA=2, ONTOLOGY_GRAPH=3, CURATED_MEMORY=4, EPHEMERAL_NOTES=5, TOOLS=6, SEAM=7
  - `ReconstructionProgress` type: current_step, steps_completed[], seam_crossed, errors[]
  - `loadMemorySequence(checkpointPath, outputDir)`: orchestrates loading in canonical order
  - SOUL.md required — halts reconstruction if missing
  - All other files optional — skipped if missing, no errors
  - Copies files from checkpoint directory to output directory
  - Sets seam_crossed=true only after all steps complete
- Added `@types/node` to amcp-recovery devDependencies, `"types": ["node"]` to tsconfig.json
- Exported `ReconstructionStep`, `loadMemorySequence`, `ReconstructionProgress` from `@amcp/recovery` index
- Added 9 test cases in `packages/amcp-recovery/src/reconstruction.test.ts`
  - ReconstructionStep: canonical values 0-7, exactly 8 steps
  - loadMemorySequence: all files present, missing SOUL fails, skips ontology, seam tracking, optional files, file copying, progress structure
- All 458 tests pass (287 amcp-core + 120 amcp-memory + 35 recovery + 16 exchange), build succeeds

## 2026-02-18: Multi-provider pinning support (prd-v1 task 23, Phase 2)
- Created `packages/amcp-core/src/multi-provider.ts` — pin to multiple services for redundancy
  - `MultiProvider` class implements `PinningProvider` interface
  - Constructor accepts `PinningProvider[]` and `MultiProviderOptions`
  - `requireAll` mode: all providers must succeed (throws if any fail)
  - `requireOne` mode (default): succeeds if at least one provider succeeds
  - `pin()`: calls all providers in parallel via `Promise.allSettled()`
  - `pinWithDetails()`: returns `MultiPinResult` with per-provider tracking
  - `unpin()`: best-effort unpin from all providers
  - `status()`: returns Pinned if any provider reports Pinned
  - `ProviderResult` type: { provider, success, cid?, error? }
  - `MultiPinResult` type: { cid, requestId, status, providerResults[] }
- Exported `MultiProvider`, `MultiProviderOptions`, `MultiPinResult`, `ProviderResult` from `@amcp/core` index
- Added 12 test cases in `packages/amcp-core/src/test/multi-provider.test.ts`
  - Interface compliance, constructor, requireOne (parallel pin, partial failure, all-fail)
  - requireAll (all succeed, any fail), result tracking, partial failure details
  - unpin from all, status aggregation
- All 470 tests pass (299 amcp-core + 120 amcp-memory + 35 recovery + 16 exchange), build succeeds

## 2026-02-18: Cross-provider CID verification (prd-v1 task 24, Phase 2)
- Created `packages/amcp-core/src/verify-pin.ts` — cross-provider CID availability checking
  - `verifyPinAcrossProviders(cid, providers[])`: checks all providers in parallel
  - `PinVerificationResult` type: { verified, providers[], contentMatch }
  - `ProviderAvailability` type: { name, available }
  - `verified`: true if at least one provider has the CID pinned
  - `contentMatch`: true if all providers agree (CIDs are content-addressed = same hash)
  - Handles unreachable providers gracefully (counts as unavailable)
- Exported `verifyPinAcrossProviders`, `PinVerificationResult`, `ProviderAvailability` from `@amcp/core` index
- Added 9 test cases in `packages/amcp-core/src/test/verify-pin.test.ts`
  - All providers pinned, none pinned, partial availability, content match semantics
  - Unreachable providers, result structure, single provider, empty provider list
- All 479 tests pass (308 amcp-core + 120 amcp-memory + 35 recovery + 16 exchange), build succeeds

## 2026-02-18: Extend CheckpointMetadata with pinning provider info (prd-v1 task 25, Phase 2)
- Extended `CheckpointMetadata` in `packages/amcp-memory/src/checkpoint.ts`
  - Added `pinningProviders?: PinningProviderEntry[]` — tracks which providers stored the content
  - Added `primaryProvider?: string` — which provider to use for retrieval
  - `PinningProviderEntry` interface: { name, cid, requestId? }
  - All fields optional — backward compatible with existing checkpoints
- Exported `PinningProviderEntry` from `@amcp/memory` index.ts
- Added 9 test cases in `packages/amcp-memory/test/checkpoint-providers.test.ts`
  - pinningProviders: accepted array, optional requestId, omitted when not provided
  - primaryProvider: accepted string, omitted when not provided
  - Both fields together, JSON roundtrip, signature valid, backward compatibility
- All 488 tests pass (308 amcp-core + 129 amcp-memory + 35 recovery + 16 exchange), build succeeds

## 2026-02-19: Evolution tracking in CheckpointMetadata (prd-v1 tasks 16+17, Phase 3)
- Extended `CheckpointMetadata` in `packages/amcp-memory/src/checkpoint.ts`
  - Added `evolutionChainCID?: string` — CID of evolution.jsonl containing relation inference history (A-MEM pattern)
  - Added `relatedEntitiesCount?: number` — count of entities updated by evolution engine since last checkpoint
  - Both fields optional — backward compatible with existing checkpoints
  - JSDoc comments on each field explaining purpose
- Added 9 test cases in `packages/amcp-memory/test/evolution-tracking.test.ts`
  - evolutionChainCID: accepted in metadata, omitted when not provided
  - relatedEntitiesCount: accepted values 5 and 0, omitted when not provided
  - Both fields together: accepted simultaneously, survive JSON roundtrip
  - Signature validity: checkpoints with evolution metadata verify correctly
  - Backward compatibility: existing checkpoints without evolution fields remain valid
- All 497 tests pass (308 amcp-core + 138 amcp-memory + 35 recovery + 16 exchange), build succeeds

## 2026-02-19: Temporal index utilities (prd-v1 tasks 18+19, Phase 3)
- Created `packages/amcp-memory/src/temporal-index.ts` — entity version tracking across checkpoints
  - `CheckpointSnapshot` type: input format with cid, timestamp, entities array
  - `TemporalIndexEntry` type: entity_id, checkpoint_cid, timestamp, version_hash, properties_snapshot
  - `TemporalIndex` type: Map<string, TemporalIndexEntry[]> sorted chronologically
  - `buildTemporalIndex(snapshots)`: builds per-entity timelines sorted by timestamp
  - `getEntityHistory(index, entity_id)`: returns full version timeline, empty array if not found
  - `queryByTimeRange(index, entity_id, start?, end?)`: filters by date range (inclusive)
  - `computeVersionHash()`: SHA-256 of canonical JSON for change detection
- Exported all types and functions from `@amcp/memory` index.ts
- Added 10 test cases in `packages/amcp-memory/test/temporal-index.test.ts`
  - buildTemporalIndex: 3 snapshots sorted, deterministic output, empty input
  - getEntityHistory: 2 versions, non-existent entity, unchanged properties detection
  - queryByTimeRange: date filtering, entity after range, no bounds, timezone handling
- All 507 tests pass (308 amcp-core + 148 amcp-memory + 35 recovery + 16 exchange), build succeeds
