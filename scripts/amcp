#!/usr/bin/env bash
# AMCP CLI wrapper — resolves paths relative to install location
# Ships pre-bundled JS (no TypeScript runtime, no native deps)
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "$0" 2>/dev/null || realpath "$0" 2>/dev/null || echo "$0")")" && pwd)"
exec node "${SCRIPT_DIR}/amcp-cli.mjs" "$@"
